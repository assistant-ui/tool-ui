"use client";

export { Button } from "../ui/button";
export {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "../ui/dropdown-menu";
export {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "../ui/tooltip";
export { Badge } from "../ui/badge";

// We will use plain <img>/<video> elements with CSS aspect-ratio, so no extra UI deps required.
