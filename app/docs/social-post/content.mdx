import { DocsHeader } from "../_components/docs-header";
import { XPost } from "@/components/tool-ui/x-post";
import { InstagramPost } from "@/components/tool-ui/instagram-post";
import { LinkedInPost } from "@/components/tool-ui/linkedin-post";
import { DataTable } from "@/components/tool-ui/data-table";

<DocsHeader title="Social Posts" mdxPath="app/docs/social-post/content.mdx" />

A family of platform-specific components for rendering social media content: `XPost`, `InstagramPost`, and `LinkedInPost`. Each component is optimized for its platform's visual style while sharing a consistent API pattern.

<Tabs items={["X Post", "Instagram Post", "LinkedIn Post"]}>
  <Tab>
    <div className="not-prose w-[540px]">
      <XPost
        post={{
          id: "post-1",
          author: {
            name: "Ada Lovelace",
            handle: "ada",
            avatarUrl: "https://api.dicebear.com/7.x/adventurer/svg?seed=Ada",
            verified: true,
          },
          text: "Thinking about how to make Tool UIs feel more like collaboration than chrome.",
          stats: { likes: 12 },
          createdAt: "2025-01-05T12:00:00.000Z",
        }}
        footerActions={[
          { id: "open", label: "Open in app" },
        ]}
      />
    </div>
  </Tab>
  <Tab>
    <div className="not-prose w-[540px]">
      <InstagramPost
        post={{
          id: "post-2",
          author: {
            name: "Ada Lovelace",
            handle: "ada",
            avatarUrl: "https://api.dicebear.com/7.x/adventurer/svg?seed=Ada",
            verified: true,
          },
          text: "Golden hour vibes",
          media: [{
            type: "image",
            url: "https://images.unsplash.com/photo-1477959858617-67f85cf4f1df?w=800&h=800&fit=crop",
            alt: "City skyline",
          }],
          stats: { likes: 842 },
          createdAt: "2025-01-05T18:00:00.000Z",
        }}
      />
    </div>
  </Tab>
  <Tab>
    <div className="not-prose w-[540px]">
      <LinkedInPost
        post={{
          id: "post-3",
          author: {
            name: "Ada Lovelace",
            avatarUrl: "https://api.dicebear.com/7.x/adventurer/svg?seed=Ada",
            headline: "Engineering Lead | Building the future",
          },
          text: "Excited to share that our team shipped a major update today. Six months of hard work paid off!",
          stats: { likes: 234 },
          createdAt: "2025-01-05T09:00:00.000Z",
        }}
      />
    </div>
  </Tab>
</Tabs>

## Components

This is a collection of three separate, platform-specific components:

<div className="not-prose my-6">
  <DataTable
    columns={[
      { key: "component", label: "Component" },
      { key: "import", label: "Import" },
      { key: "useCase", label: "Use Case" },
    ]}
    data={[
      { component: "XPost", import: "@/components/tool-ui/x-post", useCase: "Twitter/X posts, quoted tweets" },
      { component: "InstagramPost", import: "@/components/tool-ui/instagram-post", useCase: "Instagram posts, image carousels" },
      { component: "LinkedInPost", import: "@/components/tool-ui/linkedin-post", useCase: "LinkedIn posts, professional content" },
    ]}
  />
</div>

Each component is independent and can be imported separately, allowing for tree-shaking when you only need one platform.

## When to Use

- **Good for:** Social media posts, activity feeds, external messages
- **Not for:** Dense event tables, editable rich text

## Key Features

- **Platform-native styling**: Each component mirrors its platform's visual language
- **Simple props**: Flat `post` object, no complex configuration
- **Footer actions**: Add custom actions below any post via `footerActions`
- **Lightweight**: ~250 lines per component, no shared context or state management

## Source and Install

Copy the platform component(s) you need into your project:

<Files>
  <Folder name="components" defaultOpen>
    <Folder name="tool-ui" defaultOpen>
      <Folder name="x-post">
        <File name="x-post.tsx" />
        <File name="schema.ts" />
        <File name="index.ts" />
      </Folder>
      <Folder name="instagram-post">
        <File name="instagram-post.tsx" />
        <File name="schema.ts" />
        <File name="index.ts" />
      </Folder>
      <Folder name="linkedin-post">
        <File name="linkedin-post.tsx" />
        <File name="schema.ts" />
        <File name="index.ts" />
      </Folder>
    </Folder>
  </Folder>
</Files>

## Usage

```tsx
import { XPost } from "@/components/tool-ui/x-post";
import { InstagramPost } from "@/components/tool-ui/instagram-post";
import { LinkedInPost } from "@/components/tool-ui/linkedin-post";

// X Post
<XPost
  post={{
    id: "tweet-123",
    author: { name: "Jane", handle: "jane", avatarUrl: "...", verified: true },
    text: "Hello world!",
    stats: { likes: 42 },
    createdAt: "2025-01-01T12:00:00Z",
  }}
  onAction={(action, post) => console.log(action, post.id)}
  footerActions={[{ id: "report", label: "Report", variant: "destructive" }]}
  onFooterAction={(id) => console.log(id)}
/>

// Instagram Post
<InstagramPost
  post={{
    id: "ig-123",
    author: { name: "Jane", handle: "jane", avatarUrl: "..." },
    text: "Beach day!",
    media: [{ type: "image", url: "...", alt: "Beach photo" }],
    stats: { likes: 1234 },
  }}
/>

// LinkedIn Post
<LinkedInPost
  post={{
    id: "li-123",
    author: { name: "Jane Smith", avatarUrl: "...", headline: "CEO at Acme" },
    text: "Excited to announce...",
    linkPreview: { url: "...", title: "News", domain: "acme.com" },
    stats: { likes: 89 },
  }}
/>
```

## Props

All three components share the same prop pattern:

<TypeTable
  type={{
    post: { description: "Platform-specific post data", type: "XPostData | InstagramPostData | LinkedInPostData", required: true },
    className: { description: "Additional CSS classes", type: "string" },
    onAction: { description: "Called when like/share clicked", type: "(action: string, post) => void" },
    footerActions: { description: "Custom actions below the post", type: "Action[]" },
    onFooterAction: { description: "Called when footer action clicked", type: "(actionId: string) => void" },
  }}
/>

### XPostData

<TypeTable
  type={{
    id: { description: "Unique identifier", type: "string", required: true },
    author: { description: "Author info", type: "{ name, handle, avatarUrl, verified? }", required: true },
    text: { description: "Post content", type: "string" },
    media: { description: "Attached media", type: "{ type, url, alt, aspectRatio? }[]" },
    linkPreview: { description: "Link card", type: "{ url, title?, description?, imageUrl?, domain? }" },
    quotedPost: { description: "Quoted tweet", type: "XPostData" },
    stats: { description: "Engagement", type: "{ likes? }" },
    createdAt: { description: "ISO timestamp", type: "string" },
  }}
/>

### InstagramPostData

<TypeTable
  type={{
    id: { description: "Unique identifier", type: "string", required: true },
    author: { description: "Author info", type: "{ name, handle, avatarUrl, verified? }", required: true },
    text: { description: "Caption", type: "string" },
    media: { description: "Images/videos", type: "{ type, url, alt }[]" },
    stats: { description: "Engagement", type: "{ likes? }" },
    createdAt: { description: "ISO timestamp", type: "string" },
  }}
/>

### LinkedInPostData

<TypeTable
  type={{
    id: { description: "Unique identifier", type: "string", required: true },
    author: { description: "Author info", type: "{ name, avatarUrl, headline? }", required: true },
    text: { description: "Post content", type: "string" },
    media: { description: "Attached media", type: "{ type, url, alt }[]" },
    linkPreview: { description: "Link card", type: "{ url, title?, description?, imageUrl?, domain? }" },
    stats: { description: "Engagement", type: "{ likes? }" },
    createdAt: { description: "ISO timestamp", type: "string" },
  }}
/>
