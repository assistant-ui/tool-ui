import { DocsHeader } from "../_components/docs-header";
import { XPost } from "@/components/tool-ui/x-post";

<DocsHeader
  title="X Post"
  description="Platform-native post card for X (Twitter)."
  mdxPath="app/docs/x-post/content.mdx"
/>

## Getting Started

<Steps>

<Step title="Install the component">

Run this once from your project root.

```bash
npx shadcn@latest add https://tool-ui.com/r/x-post.json
```

</Step>

<Step title="Use it in your application">

Render `XPost` in your UI with tool-compatible props.

<div className="[&_pre]:max-h-[30rem] [&_pre]:overflow-y-auto">

```tsx
import { XPost } from "@/components/tool-ui/x-post";

export function Example() {
  return (
    <XPost
      post={{
        id: "x-post-1",
        author: {
          name: "Taylor Kim",
          handle: "taylorkim",
          avatarUrl: "https://example.com/avatar.jpg",
          verified: true,
        },
        text: "Thinking about how to make Tool UIs feel more collaborative.",
        stats: { likes: 12 },
        createdAt: "2025-01-05T12:00:00.000Z",
      }}
    />
  );
}
```

</div>

</Step>

<Step title="Register it in your runtime provider">

Register this renderer so tool results display as `XPost`.

```tsx
import { type Toolkit } from "@assistant-ui/react";
import { XPost, safeParseSerializableXPost } from "@/components/tool-ui/x-post";
import { createResultToolRenderer } from "@/components/tool-ui/shared";

export const toolkit: Toolkit = {
  showXPost: {
    type: "backend",
    render: createResultToolRenderer({
      safeParse: safeParseSerializableXPost,
      render: (parsedResult) => <XPost post={parsedResult} />,
    }),
  },
};
```

</Step>

</Steps>

These snippets use [assistant-ui](https://assistant-ui.com) for end-to-end wiring, but the XPost component itself is framework-agnostic at the UI layer: you can use it in any React codebase with any LLM SDK or tool-calling interface that can provide compatible props.

## Key Features

<FeatureGrid>
  <Feature icon="Paintbrush" title="Platform-native styling">
    Matches X visual language and interaction patterns
  </Feature>
  <Feature icon="MessageSquare" title="Rich post content">
    Supports text, quoted posts, media, and link previews
  </Feature>
  <Feature icon="MousePointerClick" title="Response actions">
    Add custom actions below each post
  </Feature>
  <Feature icon="ShieldCheck" title="Safe links">
    Built-in safe navigation helpers for external URLs
  </Feature>
</FeatureGrid>

## Props

<TypeTable
  type={{
    post: {
      description: "Post payload validated by `SerializableXPostSchema`",
      type: "XPostData",
      required: true,
    },
    className: {
      description: "Optional className for the outer card",
      type: "string",
    },
    onAction: {
      description: "Fires when the user triggers a built-in post action",
      type: "(action: string, post: XPostData) => void",
    },
    responseActions: {
      description: "Optional custom response actions",
      type: "Action[]",
    },
    onResponseAction: {
      description: "Fires when a response action is selected",
      type: "(actionId: string) => void | Promise<void>",
    },
    onBeforeResponseAction: {
      description: "Optional preflight guard before a response action runs",
      type: "(actionId: string) => boolean | Promise<boolean>",
    },
  }}
/>
